{
  "name": "rttype",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "build" : "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael S Munson",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
